# Levit — Multi-Stakeholder Furniture Decision Helper

다중 이해관계자(가족, 동료 등)의 선호를 입력받아, 식탁 세트를 타협적으로 추천해 주는 MVP 웹 앱입니다.
---

## 전체 데이터 흐름 요약
데이터는 IKEA 웹사이트에서 크롤링해왔습니다.
```
[프론트엔드]                    [백엔드]                           [데이터]
     │                             │                                  │
     │ 1. 사용자 입력               │                                   │
     │   - 참가자 수 → Person A/B/C │                                  │
     │   - 4개 슬라이더 + 메모      │                                   │ 
     │   - 사전 합의 내용 (예산, 좌석 수, 등)│                            │
     │                             │                                  │
     │ 2. POST /api/consensus-...  │                                  │
     │   { constraints, stakeholders }                               │
     │ ──────────────────────────> │                                  │
     │                             │ 3. 제약만으로 넓은 후보 풀 (최대 50) │
     │                             │    → Multi-agent용 후보           │
     │                             │                                  │
     │                             │ 4. Stage 1: 인당 슬라이더 1차 필터 │
     │                             │    → FAISS(notes)로 5개 선정      │
     │                             │    → LLM이 reasoning만 생성       │
     │                             │    → stakeholder_opinions         │
     │                             │                                  │
     │                             │ 5. Stage 2: Orchestrator          │
     │                             │    CoT + compromised_preference  │
     │                             │    (제품 선정 없음)                │
     │                             │                                  │
     │                             │ 6. DB: 타협 preference로 필터+점수 │
     │                             │    필터 + 점수 → 상위 10명 후보    │
     │                             │                                  │
     │                             │ 7. FAISS: keywords_or_themes로    │
     │                             │    시맨틱 랭킹 → 최종 3명          │
     │                             │    → recommendations              │
     │                             │                                  │
     │ <────────────────────────── │ 8. ID→이름 치환(문자열만) 후 전송  │
     │   { stakeholder_opinions,   │    (product_id/local_ranking 유지) │
     │     consensus }             │                                  │
     │                             │                                  │
     │ 9. 결과 화면                 │                                  │
     │   - Person A/B/C별 의견      │                                  │
     │   - 타협 설명 (CoT)          │                                  │
     │   - 최종 추천 3개 (FAISS 랭킹) │                                  │
     │                             │                                  │
```

---

## 1. 프론트엔드에서 데이터가 어떻게 들어오는지

### 입력 화면 (PreferenceInput.jsx)

| 입력 항목 | 설명 | API로 전달되는 키 |
|----------|------|-------------------|
| 참가자 수 (1~10) | Person A, B, C … 박스 개수 | `stakeholders.length` |
| Person별 4개 슬라이더 (1~5) | 얼룩/관리, 밝고 모던, 공간 동선, 착석 여유 | `stakeholders[].preferences` (`maintenance_ease`, `bright_modern`, `space_flow`, `seat_comfort`) |
| Person별 자유 메모 | "그 외에 중요하게 생각하는 부분" | `stakeholders[].notes` |
| 예산 min/max (KRW) | 최소·최대 예산 | `constraints.budget_min`, `constraints.budget_max` |
| 최소 좌석 수 | N인용 이상 | `constraints.seats_needed` |
| 아이/안전 고려 | 체크 시 안전 인증 제품만 | `constraints.child_safe_required` |
| 조립 복잡한 제품 피하기 | 체크 시 조립 필요 제품 제외 | `constraints.assembly_hassle_sensitive` |

**전송 시점**: 사용자가 **「Get Consensus」** 버튼을 누르면  
`App.jsx`의 `handleGetConsensus(payload)`가 호출되고,  
`POST /api/consensus-and-recommendations`로 아래 형태의 JSON이 전송됩니다.

```json
{
  "constraints": {
    "budget_min": 200000,
    "budget_max": 600000,
    "seats_needed": 4,
    "child_safe_required": false,
    "assembly_hassle_sensitive": false
  },
  "stakeholders": [
    {
      "id": "A",
      "label": "Person A",
      "preferences": {
        "maintenance_ease": 4,
        "bright_modern": 3,
        "space_flow": 2,
        "seat_comfort": 5
      },
      "notes": "아이 있는 집이라 모서리·소재 안전했으면 좋겠어요."
    }
  ]
}
```

---

## 2. 백엔드에서 어떤 로직을 타는지

서버는 **server.js**에서 `POST /api/consensus-and-recommendations`를 처리할 때 아래 순서로 동작합니다.

### 2.1 제품 데이터 준비 (서버 기동 시)

- **vectorIndex.js**의 `initVectorIndex()`가 한 번 실행됩니다.
- **dataLoader.js**로 **`data/final_data.csv`**를 읽어 제품 목록을 만듭니다.  
  - UTF-8 BOM이 있으면 제거해 첫 컬럼이 `product_name`으로 인식되도록 합니다.  
  - `product_id`, `product_name`은 문자열로 유지합니다 (`cast: false`).
- 각 제품의 `index_text`를 OpenAI **text-embedding-3-small**로 임베딩해,  
  FAISS(또는 in-memory) 벡터 인덱스를 만듭니다.
- `getAllProducts()`로 이 제품 목록을 가져와 **products** 변수에 넣고, 이후 모든 API가 이 목록을 사용합니다.

즉, **현재 쓰는 데이터는 모두 `data/final_data.csv` 한 군데**입니다.

### 2.2 요청 처리 흐름 (POST 시)

1. **제약 필터 (Hard filter)**  
   `products` 중에서 다음을 만족하는 것만 남깁니다.  
   - `category === "dining_table_set"`  
   - `seats_recommended >= constraints.seats_needed`  
   - `price_krw`가 `budget_min` ~ `budget_max` 사이  
   - `child_safe_required === true`이면 `is_child_safe === true`만  
   - `assembly_hassle_sensitive === true`이면 `has_assembly_required === false`만  
   - 상위 **50개**를 Stage 1 후보로 둡니다.

2. **Stage 1 — 이해관계자별 의견**  
   - **인당**: `scoring.js`의 `scoreProduct(product, [그 사람])`로 1차 필터 → 점수 순 상위 25개.  
   - **FAISS**: 그 25개 안에서 **notes**(또는 기본 쿼리)로 시맨틱 검색 → **상위 5개**를 그 사람의 추천으로 확정.  
   - **openaiClient.generateReasoningForStakeholderOpinions(stakeholders, opinionsWithProducts)**  
     - 이미 정해진 5개 제품에 대해 “왜 이 제품들이 맞는지” **reasoning**만 생성 (제품 선정은 하지 않음).  
   - 응답: Person별  
     - **local_ranking**: product_id 배열 (5개)  
     - **local_ranking_product_names**: 제품명 배열  
     - **reasoning**: 한국어 설명  

3. **Stage 2 — 타협 정리 (Orchestrator)**  
   - **buildPreferenceSummary(constraints, stakeholders)**  
     - 공통 조건 + Person별 슬라이더 + notes를 한국어로 요약.  
   - **openaiClient.callChatGPTForConsensus(preferenceSummary, stakeholderOpinions)**  
     - 타협 과정(CoT) + **compromised_preference** JSON만 생성. (제품 추천은 하지 않음.)  
   - 응답: **consensus_explanation**, **compromised_preference** (budget, seats, 4개 슬라이더, keywords_or_themes 등).

4. **타협 preference로 최종 추천**  
   - DB: `compromised_preference`로 필터 + `scoreProduct(·, [타협 에이전트])` 점수 → **상위 10명**.  
   - **FAISS**: `keywords_or_themes`(또는 CoT 앞부분)로 시맨틱 검색 → **최종 3명** → `recommendations` (product_id, rank, reason, caution).

5. **응답 전 치환**  
   - **replaceProductIdsWithNamesInPayload(payload, products)**  
     - 문자열 필드(reasoning, consensus_explanation, reason, caution 등) 안의 product_id를 product_name으로 치환.  
     - **product_id**, **local_ranking** 필드는 치환하지 않아, 프론트에서 id로 제품 조회 가능.

6. **응답 JSON**  
   위 결과를 묶어서 `{ stakeholder_opinions, consensus }` 형태로 내려줍니다.

```json
{
  "stakeholder_opinions": [
    {
      "label": "Person A",
      "local_ranking": ["095.994.95", "496.174.40", "..."],
      "local_ranking_product_names": ["VIHALS 비할스 ...", "GRÖTÅN 그뢰톤 ...", "..."],
      "reasoning": "추천된 VIHALS 비할스와 GRÖTÅN 그뢰톤은 ..."
    }
  ],
  "consensus": {
    "consensus_explanation": "타협 과정 설명 (문자열 내 ID는 제품명으로 치환됨)",
    "compromised_preference": {
      "budget_min": 200000,
      "budget_max": 550000,
      "seats_needed": 4,
      "maintenance_ease": 3,
      "bright_modern": 4,
      "space_flow": 2,
      "seat_comfort": 4,
      "keywords_or_themes": "밝고 모던, 관리 쉬운 소재, ..."
    },
    "recommendations": [
      { "product_id": "496.174.40", "rank": 1, "reason": "...", "caution": "..." }
    ]
  }
}
```

---

## 3. 결과가 다시 프론트엔드로 어떻게 나오는지

### App.jsx

- `POST`가 성공하면 응답 JSON을 **consensusResult**에 저장하고, 화면을 **results** 뷰로 전환합니다.
- **Results** 컴포넌트에 `result={consensusResult}`로 넘깁니다.

### Results.jsx

- **result.stakeholder_opinions**  
  - Person A, B, C … 각각에 대해  
    - **선호 제품**: `local_ranking_product_names`로 제품명 나열  
    - **1순위**: `product_id`로 GET /api/products 목록에서 조회한 제품명·가격 (없으면 `local_ranking_product_names[0]`)  
    - **reasoning** (문자열 내 ID는 이미 백엔드에서 제품명으로 치환된 상태)  
  을 한 블록씩 보여줍니다.  

- **result.consensus.consensus_explanation**  
  - 타협 설명 전체를 텍스트로 표시합니다. (문자열 내 ID는 제품명으로 치환됨.)  

- **result.consensus.recommendations**  
  - 타협 preference + FAISS 시맨틱 랭킹으로 뽑은 Top 3 제품을 카드로 보여줍니다.  
  - 각 카드: **product_id**로 제품 조회 → 제품명, 이미지, 가격, 평점, 리뷰 수, seats, material, color, style_tags  
  - **reason**, **caution** 함께 표시.  

제품 상세는 **GET /api/products?limit=5000**으로 한 번 불러온 뒤,  
`product_id`(및 점 제거·variant)로 매칭해서 사용합니다.

---

## 4. 한 줄로 정리

- **데이터 소스**: `data/final_data.csv` (BOM 제거, product_id/name 문자열 유지) → dataLoader → vectorIndex → **products**  
- **프론트**: 사용자 입력(constraints + stakeholders) → **POST /api/consensus-and-recommendations**  
- **백엔드**: 제약 필터 50명 → **Stage 1** 인당 슬라이더 필터 + FAISS(notes)로 5개 선정 → LLM reasoning 생성 → **Stage 2** Orchestrator(CoT + compromised_preference) → 타협 preference로 필터·점수 10명 → **FAISS** 시맨틱 랭킹 3명 → 문자열만 ID→이름 치환 후 **stakeholder_opinions + consensus** 전송  
- **프론트**: Person별 의견(선호 제품명, 1순위, reasoning) + 타협 설명 + 최종 추천 3개 카드(제품명·이미지·메타) 표시.

---

## 5. 실행 방법

```bash
# Conda 환경 (권장)
conda activate levit

# 백엔드 (포트 3001)
cd backend && npm install && npm run dev

# 프론트엔드 (포트 5173, /api → 백엔드 프록시)
cd frontend && npm install && npm run dev
```

브라우저에서 **http://localhost:5173** 접속 후, 참가자 수와 슬라이더·메모를 입력하고 **Get Consensus**를 누르면 위 흐름대로 결과가 나옵니다.

---

